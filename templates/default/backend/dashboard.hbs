
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{{pageTitle}}</title>

        <!-- Bootstrap CSS -->
        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet">

        <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
        <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
        <!--[if lt IE 9]>
            <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.3/html5shiv.js"></script>
            <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
        <![endif]-->
    </head>
    <body>
        <nav class="navbar navbar-default" role="navigation">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="/">{{__ 'Classifield Website'}}</a>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse navbar-ex1-collapse">
                <ul class="nav navbar-nav">
                    <li class="active"><a href="#">{{__ 'Ads'}}</a></li>
                    <li><a href="#">{{__ 'Categories'}}</a></li>
                </ul>
                <form class="navbar-form navbar-left" role="search">
                    <div class="form-group">
                        <input type="text" class="form-control" placeholder="{{__ 'Search'}}">
                    </div>
                    <button type="submit" class="btn btn-default">{{__ 'Submit'}}</button>
                </form>

                <ul class="nav navbar-nav">
                    <li>
                    <p class="navbar-text" style="color: green;font-weight: bold;">{{__n '%d member' 2}}</p>
                    </li>
                </ul>

                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><img src="/images/{{clanguage}}.png"> {{@root.__ clanguage}} <b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            {{#each languages}}
                            <li><a onclick="window.location.href='/{{this}}'" href="#"><img src="/images/{{this}}.png"> {{@root.__ this}}</a></li>
                            {{/each}}
                        </ul>
                    </li>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{member.local.email}} <b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            {{#if logged}}
                            <div style="padding: 0 0 5px 20px;border-bottom: 1px solid #ccc;font-weight: bold;">{{member.info.firstname}} {{member.info.lastname}}</div>
                            <li><a href="/backoffice/tai-khoan">{{__ 'Profile'}}</a></li>
                            <li><a href="/backoffice/logout">{{__ 'Logout'}}</a></li>
                            {{else}}
                            <li><a href="/thanh-vien/dang-nhap">{{__ 'Login'}}</a></li>
                            <li><a href="/thanh-vien/dang-ky">{{__ 'Register'}}</a></li>
                            {{/if}}
                        </ul>
                    </li>
                </ul>
            </div><!-- /.navbar-collapse -->
        </nav>

        <div class="container">
            <h1 class="text-center">Hello, Administrator</h1>
            <div class="col-md-6 col-md-offset-3">
                <table class="table table-bordered">
                    <tbody>
                        <tr>
                            <td>Full Name: </td>
                            <td>{{member.info.firstname}} {{member.info.lastname}}</td>
                        </tr>

                        <tr>
                            <td>Email address: </td>
                            <td>{{member.local.email}}</td>
                        </tr>

                        <tr>
                            <td>Join Date: </td>
                            <td>{{format_date member.createdAt 'L LTS'}}</td>
                        </tr>

                        <tr>
                            <td>Role: </td>
                            <td>
                                {{#ifCond member.roles "==" "ADMIN"}}
                                    Administrator
                                {{/ifCond}}
                                {{#ifCond member.roles "==" "MOD"}}
                                    Mod
                                {{/ifCond}}
                            </td>
                        </tr>

                    </tbody>
                </table>
            </div>
        </div>
        <!-- jQuery -->
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
        <!-- Bootstrap JavaScript -->
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    </body>
</html>
